
# PRIMA Updater

Программа для автоматического обновления приложения PRIMA с сетевого сервера с поддержкой резервного копирования и восстановления.

## Описание

PRIMA Updater - это утилита для синхронизации локальной установки PRIMA с серверной версией. Программа обеспечивает:

- 🔄 Автоматическое сравнение локальных и серверных файлов
- 💾 Создание резервных копий перед обновлением
- 📊 Интерактивный интерфейс
- 🔍 Детальное логирование всех операций
- ⚡ Выборочное или полное обновление файлов
- 🔙 Восстановление из резервных копий
- 🎯 Автоматическое обновление ярлыков на рабочем столе

## Требования

- Python 3.7 или выше
- Git (Клонирование репозитория и обновление)
- Windows (использует `msvcrt` и ярлыки)
- Доступ к сетевой папке с PRIMA


## Установка

### 1. Установка Python

Если Python не установлен, скачайте его с [python.org](https://www.python.org/downloads/)

### 2. Установка Git

Скачайте и установите Git с [git-scm.com](https://git-scm.com/download/win). Это необходимо для клонирования репозитория.

После установки Git будет доступен в командной строке и PowerShell.

### 3. Клонирование репозитория

### 2. Клонирование репозитория

```bash
git clone https://github.com/DiDaimon/PRIMA_update.git
cd PRIMA_update
```

### 3. Установка зависимостей

#### Автоматическая установка

Запустите скрипт:

```bash
prima_update.bat
```

Скрипт установки:
- Проверит наличие Python и pip
- Установит все необходимые зависимости из `requirements.txt`
- Поддерживает установку через прокси-сервер при необходимости
- Создаст ярлык на рабочем столе (опционально)

#### Ручная установка

```bash
cd ПАПКА_СО_СКРИПТОМ
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
```

## Зависимости

Программа использует следующие библиотеки:

- **rich** - красивый вывод в консоль, таблицы, прогресс-бары, интерактивные запросы
- **art** - ASCII-арт для заголовков

Полный список в файле `requirements.txt`.

## Использование

### Запуск программы

```bash
prima_update.bat
```

#### Через ярлык

Дважды щелкните по файлу `[PRIMA] xx.xx.xxx.lnk` на рабочем столе.

### Основной рабочий процесс

1. **Проверка сервера** - программа проверяет доступность сетевой папки
2. **Сравнение файлов** - сравнивает локальные файлы с серверными
3. **Отображение изменений** - показывает список измененных и отсутствующих файлов
4. **Выбор действия** - интерактивное меню с доступными операциями
5. **Выполнение операции** - синхронизация, резервное копирование или восстановление

### Интерактивное меню

После запуска доступны следующие действия:

1. **Обновить всё** - синхронизирует все измененные и отсутствующие файлы
2. **Обновить только изменённые** - обновляет только измененные файлы
3. **Обновить только отсутствующие** - копирует только отсутствующие файлы
4. **Полное копирование** - перепишет все файлы в локальной папке
5. **Восстановить из бэкапа** - восстанавливает PRIMA.exe из резервной копии
0. **Выход** - завершение работы программы

## Структура проекта

```
PRIMA_update/
├── assets/                      # Ресурсы (иконки, ярлыки)
│   ├── 1.ico                   # Иконка приложения
│   └── [PRIMA] 00.00.00.lnk    # Шаблон ярлыка
├── prima_updater/              # Основной пакет
│   ├── __init__.py             # Инициализация пакета
│   ├── backup.py               # Управление резервными копиями
│   ├── config.py               # Конфигурация приложения
│   ├── install.py              # Установка зависимостей
│   ├── logger.py               # Настройка логирования
│   ├── server.py               # Проверка доступности сервера
│   ├── sync.py                 # Синхронизация файлов
│   ├── ui.py                   # Пользовательский интерфейс
│   └── rich_console.py         # Настройка Rich консоли
├── logs/                       # Логи (создается автоматически)
├── backups/                    # Резервные копии (создается автоматически)
├── prima_update.py             # Главный скрипт
├── prima_update.bat            # BAT-файл для запуска
├── requirements.txt            # Зависимости Python
├── .gitignore                  # Игнорируемые файлы Git
└── README.md                   # Документация
```

## Модули

### prima_update.py

Главный модуль приложения. Координирует работу всех компонентов:

- Проверка зависимостей
- Инициализация конфигурации и логирования
- Проверка доступности сервера
- Сравнение директорий
- Отображение меню и обработка действий пользователя

**Основные функции:**
- `check_dependencies()` - проверка установленных зависимостей
- `wait_for_keypress(timeout)` - ожидание нажатия клавиши
- `main()` - главная функция программы

### prima_updater/config.py

Централизованное хранение настроек приложения.

**Класс Config:**
- `SERVER_DIRECTORY` - путь к сетевой папке PRIMA
- `LOCAL_DIRECTORY` - локальный путь к PRIMA (из переменной окружения)
- `IGNORE_LIST` - список игнорируемых файлов/папок
- `PRIMA_EXE` - имя исполняемого файла
- `LOGS_DIRECTORY` - директория для логов
- `BACKUPS_DIRECTORY` - директория для резервных копий

### prima_updater/logger.py

Настройка системы логирования.

**Функция setup_logging:**
- Создает директорию для логов
- Настраивает форматирование сообщений
- Создает файловый и консольный обработчики
- Возвращает настроенный логгер

### prima_updater/server.py

Проверка доступности сервера и валидация путей.

**Функции:**
- `check_server_availability(server_path)` - проверка доступности сетевой папки
- `validate_paths(server_dir, local_dir, logger)` - валидация путей

### prima_updater/sync.py

Синхронизация файлов между сервером и локальной директорией.

**Класс FileSync:**
- `compare_directories()` - сравнение директорий
- `copy_files(files_list)` - копирование файлов
- `_should_ignore(file_path)` - проверка игнорирования файла
- `_copy_file(src, dst)` - копирование одного файла

### prima_updater/backup.py

Управление резервными копиями PRIMA.exe.

**Класс BackupManager:**
- `create_backup()` - создание резервной копии
- `restore_backup(backup_path)` - восстановление из копии
- `get_backups()` - получение списка резервных копий
- `cleanup_old_backups()` - очистка старых копий по правилам хранения

**Правила хранения резервных копий:**
- Текущий месяц: все копии
- Текущий год: по одной копии на месяц
- Старше года: по одной копии на год

### prima_updater/ui.py

Пользовательский интерфейс на базе Rich.

**Класс UserInterface:**
- `show_header()` - отображение заголовка
- `show_changes(diff_files, only_files)` - отображение изменений в виде таблиц
- `show_main_menu(has_changes)` - главное меню
- `show_backup_menu(backups)` - меню выбора резервной копии
- `show_restore_filters(older_years)` - меню фильтров восстановления

### prima_updater/install.py

Установка зависимостей и создание ярлыков.

**Функции:**
- `get_proxy_credentials()` - запрос данных прокси
- `check_python()` - проверка Python и pip
- `install_requirements(proxy_config)` - установка зависимостей
- `create_desktop_shortcut(script_path)` - создание ярлыка
- `setup()` - главная функция установки

## Конфигурация

### Путь к серверу

По умолчанию: `\\tserver1\RSU\PRIMA`

Изменить можно в файле `prima_updater/config.py`:

```python
SERVER_DIRECTORY = r"\\your_server\path\to\PRIMA"
```

### Игнорируемые файлы

Список файлов и папок, которые не синхронизируются:

```python
IGNORE_LIST = [
    'tract',
    'PRIMA.ini',
    'Servers.ini',
    'DTConfig.stc',
    'DebugTranslate.log',
    'PRIMA.ethl.log',
    'PRIMA.mport.log'
]
```

## Логирование

Все операции записываются в лог-файлы в директории `logs/`:

- Формат имени: `prima_update_YYYYMMDD_HHMMSS.log`
- Уровень логирования: INFO и выше
- Формат записи: `[YYYY-MM-DD HH:MM:SS] [LEVEL] Message`

**Примеры записей:**

```
[2024-01-15 10:30:45] [INFO] Запуск программы обновления PRIMA
[2024-01-15 10:30:46] [INFO] Сервер доступен
[2024-01-15 10:30:47] [INFO] Найдено изменений: 5
[2024-01-15 10:30:50] [INFO] Создан бэкап: PRIMA[15.01.24].exe
```

## Резервное копирование

### Автоматическое создание

Резервная копия создается автоматически перед каждым обновлением PRIMA.exe.

### Формат имени

`PRIMA[DD.MM.YY].exe` - где DD.MM.YY - дата изменения файла

### Правила хранения

- **Текущий месяц**: хранятся все копии
- **Текущий год**: одна копия на месяц (самая новая)
- **Старше года**: одна копия на год (самая новая)

### Восстановление

Восстановить PRIMA.exe можно через меню программы с применением фильтров:
- Текущий месяц
- Текущий год
- Старше (с выбором года)

## Устранение неполадок

### Программа не находит PRIMA

**Проблема:** Ошибка "Переменная окружения PRIMA_DIR не установлена"

**Решение:** Установите переменную окружения `PRIMA_DIR`:
```cmd
setx PRIMA_DIR "C:\Path\To\PRIMA"
```
После установки перезапустите терминал/командную строку.

### Сервер недоступен

**Проблема:** "Сервер недоступен или пути невалидны"

**Решение:**
1. Проверьте сетевое подключение
2. Убедитесь, что путь `\\tserver1\RSU\PRIMA` доступен
3. Проверьте права доступа к сетевой папке

### Ошибка импорта модулей

**Проблема:** `ImportError: No module named 'rich'`

**Решение:** Установите зависимости:
```bash
pip install -r requirements.txt
```

### Не отображаются цвета

**Проблема:** Консоль не поддерживает ANSI-цвета

**Решение:** Используйте современный терминал:
- Windows Terminal
- PowerShell 7+
- ConEmu

## Лицензия

Этот проект разработан для внутреннего использования.

## Автор

PRIMA Updater Team

## Поддержка

При возникновении проблем:
1. Проверьте логи в директории `logs/`
2. Убедитесь, что все зависимости установлены
3. Проверьте настройки в `prima_updater/config.py`
4. Обратитесь к разделу "Устранение неполадок"

---

**Версия:** 1.2  
**Последнее обновление:** ноябрь 2025